services:
  - type: web
    name: child-tracker
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: 18
      - key: MONGODB_URI
        sync: false  # 手动在 Render 后台填入 MongoDB Atlas 连接字符串
