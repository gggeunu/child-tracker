{
  "name": "child-tracker-server",
  "version": "1.0.0",
  "description": "孩子定位追踪后端服务 - 接收安卓端上报的GPS位置，提供Web端查询API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
